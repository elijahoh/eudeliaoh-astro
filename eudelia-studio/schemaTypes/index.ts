import { portfolio } from '../../schemaTypes/portfolio'
import { post } from '../../schemaTypes/post'
import { siteSettings } from '../../schemaTypes/siteSettings'

export const schemaTypes = [portfolio, post, siteSettings]
